# MessageBus

A simple message bus which uses object class types and names for dispatching. This 
allows listeners to ether listen for specific messages, or messages that are of
a super-type or even that just implement a given interface.

## To-Do
- Unit tests
- Benchmark
